<div class="container">
  <h2 class="title__lg mb__sm">New questions</h2>
  <p class="mb">for <%= link_to "@#{@user.name}", user_path(@question.user) %></p>

  <%= render 'form', question: @question %>
</div>
